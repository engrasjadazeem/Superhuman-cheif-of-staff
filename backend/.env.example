OPENAI_API_KEY=your-openai-api-key
CSV_PATH=/Users/asjadazeem/Downloads/emails.csv
BATCH_INTERVAL_SECONDS=30
CSV_RECORD_LIMIT=100
BATCH_SIZE=10
AGENT_MAX_RETRIES=3
PORT=3001

# Fix "UNABLE_TO_GET_ISSUER_CERT_LOCALLY" / Connection error (e.g. corporate proxy). Dev only.
# DISABLE_TLS_VERIFY=1

# Disable OpenAI tracing export to avoid tracing fetch errors when TLS is flaky.
# OPENAI_AGENTS_DISABLE_TRACING=1
